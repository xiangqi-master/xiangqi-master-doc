---
title: 'Quickstart'
description: 'Start building our xiangqi master in under 5 minutes'
---

## Frontend
<Warning>This requires a node version of at least **18.x.x**. Please run `node --version` to check your node version.</Warning>
1. Clone from Git.
```shell
git clone https://github.com/xiangqi-master/xiangqi-master-frontend.git 
```

2. Install dependencies.
```shell
npm install
```

3. Set up husky.
```shell
npm run prepare 
```

4. Set up husky git hooks.
```shell
npm run prepare-husky-scripts 
```
**Note**: You should be able to see a `.husky` folder with two git hook scripts - `.husky/pre-commit` and `.husky/pre-push`. 

5. Create `.env` file from `.env.template`. Fill in variables in `.env`.
```shell
copy .env.template .env
```

6. Start the project in development node.
```shell
npm run dev 
```

7. Open the link with the port number you specify. e.g. http://localhost:3000


## Backend
*More to be added...*

## Documentation

### Set up with local node
<Warning>This requires a node version of a least **18.x.x**. Please run `node --version` to check your node version. For node version < **18.x.x**, you may opt to [set up with docker](#set-up-with-docker).</Warning>

1. Clone from Git.
```shell
git clone https://github.com/xiangqi-master/xiangqi-master-doc.git
```

2. Install dependencies.
```shell
npm install 
```

3. Create `.env` file from `.env.template`. Fill in variables in `.env`.
```shell
copy .env.template .env
```

4. Start the project in development mode.
```shell
npm run dev
```

5. Open the link with the port number you specify. e.g. http://localhost:3000


### Set up with docker
1. Clone from Git.
```shell
git clone https://github.com/xiangqi-master/xiangqi-master-doc.git
```

2. Install dependencies.
```shell
npm install 
```

3. Create `.env` file from `.env.template`. Fill in variables in `.env`.
```shell
copy .env.template .env
```

4. Build docker container.

<Warning>You may use `docker-compose` rather than `docker compose` if you are still using **Compose V1**. You may read [Migrate to Compose V2](https://docs.docker.com/compose/releases/migrate/) for more details.</Warning>
```shell
docker compose up
```

5. Open the link with the port number you specify. e.g. http://localhost:3000
